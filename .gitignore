# Entorno Python
venv/
__pycache__/
*.pyc

# Configuración local de Claude Code (no aporta al proyecto compartido)
.claude/

# Datos reales de inventario (contienen precios/proveedores reales, no compartir)
*.xlsx
*.xls

# Salidas generadas por la app
*_SI_ESAM.xlsx

# Artefactos de build de PyInstaller (se regeneran con build_exe.bat)
build/
dist/
*.spec
